algoritmo "numeros maior que 0"

var
numero,soma:inteiro
inicio
numero <- 1
soma <- numero
enquanto soma <= 200 faca
   escreval("Numero: ",numero," Soma: ",soma)
   numero <- numero + 1
   soma <- soma + numero

fimenquanto
fimalgoritmo
